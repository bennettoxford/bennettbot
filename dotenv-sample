# See comments in bennettbot/settings.py and DEVELOPERS.md
# For local dev environments, these variables can be
# configured by running ./scripts/local-setup.sh
LOGS_DIR=changeme
WRITEABLE_DIR=changeme
GCP_CREDENTIALS_PATH=changeme/gcp-credentials.json
SLACK_LOGS_CHANNEL=changeme
SLACK_BENNETT_ADMINS_CHANNEL=changeme
SLACK_TECH_SUPPORT_CHANNEL=changeme
SLACK_BOT_TOKEN=changeme
SLACK_BOT_USER_TOKEN=changeme
SLACK_APP_TOKEN=changeme
SLACK_SIGNING_SECRET=changeme
SLACK_APP_USERNAME=changeme
GITHUB_WEBHOOK_SECRET=changeme
WEBHOOK_ORIGIN=http://changeme:1234
# If you need to test jobs that call the GitHub API locally, don't set
# GITHUB_APP_CLIENT_ID/GITHUB_APP_PRIVATE_KEY - instead set a
# fine-grained PAT with readonly permissions per org you want to test against,
# e.g.OPENSAFELY_CORE_DEV_GITHUB_TOKEN - see GithubMultiOrgClient.client_for_org
# in workspace/utils/github_rest_api.py.
